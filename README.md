# ChatGPT Relay

A self-hosted GitHub-backed relay that lets ChatGPT execute bounded jobs on your own Linux machine through a repository you control.

> Initial repository bootstrap. The standalone relay implementation is being added on the `initial-release` branch.
